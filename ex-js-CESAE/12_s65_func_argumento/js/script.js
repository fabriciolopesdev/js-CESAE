
function rant(message){ // 1. função criada que aceita uma message dentro
    for(i = 1; i<4; i++){ // 2. loop criado para que aconteça 3X conforme pedido no exercício, a cada clique.
        console.log(i, message.toUpperCase()); // 3. console.log.passado tudo para maiúscula conforme método upperCase
    }
}

